# -----------------------------------------------------
# TEST 16: Vowel Counting
# -----------------------------------------------------
# String: "hello world"
# Expected count: 3
# -----------------------------------------------------

.section .data

str:
    .string "hello world"

count:
    .int -1                # output result placeholder

.section .text

    # After ex1 returns, check if count matches expected value
    movl count(%rip), %eax
    cmp $3, %eax
    je success

error:
    mov $60, %rax              # sys_exit
    mov $1, %rdi               # exit code 1 (error)
    syscall

success:
    mov $60, %rax              # sys_exit
    xor %rdi, %rdi             # exit code 0 (success)
    syscall
